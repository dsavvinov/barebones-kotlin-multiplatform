package org.jetbrains.kotlin.hardcode

/**
 * This is a marker-annotation to help readers with exploring the code of this sample.
 *
 * For the sake of simplicty, a lot of options, configuration parameters and paths are
 * hardcoded in this sample.
 *
 * However, in real life, some of those parameters are almost never configured by the user
 * and handled by the "build system" (such as paths to kotlin-compiler distribution), and some
 * are
 */
annotation class UserInput()
